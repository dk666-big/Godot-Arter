# 🎮 Godot-Arter 使用说明书

**Godot 风格一站式游戏美术工坊** —— 角色 / 序列帧 / 素材 / 抠图 / 无缝地图 / 场景天气,一站式生成、管理与 Godot 对接。

> 本说明书覆盖全部功能模块、启动方式、API 配置、Godot 导入要点与常见问题。

---

## 目录

1. [快速开始](#1-快速开始)
2. [界面总览](#2-界面总览)
3. [模块使用指南](#3-模块使用指南)
4. [API 与密钥配置](#4-api-与密钥配置)
5. [素材管理与导出](#5-素材管理与导出)
6. [Godot 导入要点](#6-godot-导入要点)
7. [数据与隐私](#7-数据与隐私)
8. [常见问题 FAQ](#8-常见问题-faq)

---

## 1. 快速开始

三种方式任选,全部无需构建:

| 方式 | 操作 | 说明 |
|---|---|---|
| **A. 本地服务(推荐)** | 项目根目录执行 `node server.mjs`(仅需 Node ≥18,无需 `npm install`),浏览器打开 `http://127.0.0.1:3080/game-art-studio` | 全功能可用,自带图片代理,跨域图床也能切片/下载 |
| **B. 直接打开 HTML** | 双击根目录 `game-art-studio.html`(或 `file:///…` 打开) | 开箱即用;绝大多数中转站(base64 优先)全功能 |
| **C. DSH 插件(可选)** | `npm install --ignore-scripts` → `npx tsdown` → 注入 DSH 后访问 DSH web 路由 | 额外获得对话区 `godot_export_manifest` 工具 |

> 自定义端口:`node server.mjs 5173` 或 `PORT=8080 node server.mjs`。

---

## 2. 界面总览

- **顶部**:应用名 · 版本状态徽标。
- **标签栏**:10 个工坊——角色工坊 / 序列帧 / 素材锻造 / 智能抠图 / 无缝大地图 / 场景工坊 / 素材总管 / 后处理 / API 配置 / 设置导出。
- **右侧栏**:Godot 导出预设速查 + 当前模块操作提示(窄屏时自动隐藏)。
- **通用按钮**:每个模块都有「⬇ 下载」「📥 入库」(入库到素材总管当前库)。

---

## 3. 模块使用指南

### 3.1 🧍 角色工坊

AI 生成角色(单视图 / 三视图 / 八方向),输出透明 PNG,可直接用于 Godot `Sprite2D`。

```
操作步骤
1. 填写「角色描述 Prompt」(例:像素风 32px 冒险家少女,红斗篷)
2. 选择 风格(像素32/像素16/Q版/二次元/写实)与 视图(单视图/三视图/八方向)
3. 选择 背景色(取色器)或勾选「透明背景(PNG)」
4. 选择 提供商(内置 OpenAI/Stability/SiliconFlow、自定义供应商、本地演示)
   若选了自定义供应商且配置了多个模型,下方会出现模型下拉,可自由切换
5. 点「✨ 生成角色」→ 预览区显示结果
6. 之后可:「⬇ PNG」下载 /「📥 入库」存素材库 /「→ 序列帧」一键送入序列帧工坊 / 点击历史缩略图回看
```

- 「📁 上传参考」可提供图生图参考图。
- 未配置任何 Key 时,选择「本地演示(无Key)」即可用 Canvas 占位图走通全流程。

### 3.2 🎞️ 序列帧工坊

精灵表自动切片、动画预览、打包成表、导出 `SpriteFrames.json`(Godot 直接用)。

```
操作步骤
1. 「上传精灵表或单帧图」(或点生成角色面板的「→ 序列帧」自动带入)
2. 设置 列(cols)× 行(rows)+ 帧率 FPS
3. 「🔪 切片」→ 下方出现逐帧缩略图;「▶ 播放」预览动画
4. 「📦 打包成表」→ 右侧得到打包图
5. 导出:勾选「行 = 方向」并填写方向名(如 down,left,up,right),
   「⬇ 导出 Godot」→ 下载打包 PNG + SpriteFrames.json
   (导出 JSON 含命名动画 down/left/up/right + STAND 复用首帧)
6. 也可在最下方用 AI 直接生成序列(输入描述 → 生成 → 自动切片)
```

### 3.3 🧱 素材锻造

多 Prompt 批量生成道具 / 图标 / 特效 + 本地程序化素材。

```
1. 「批量 Prompt」每行一个(例:像素风 金币 正面 32px / 像素风 红药水 瓶子)
2. 选择 风格(图标/像素道具/特效)、提供商、背景色(或透明)
3. 「⚡ 批量生成」→ 网格逐个生成
4. 「⬇ 打包 ZIP (PNG)」逐个下载 / 「📥 全部入库」/ 「清空」
5. 本地程序化生成(无需 API):「👤 软阴影」生成角色脚下椭圆软阴影 PNG,
   「✨ 光晕」生成径向光晕(粒子/光效可用)
```

### 3.4 ✂️ 智能抠图

一键扣背景,输出透明 PNG。

```
模式说明
- 🤖 智能自动:自动分析四边背景色并扩散扣除,适合产品图/素材图
- 🪄 魔棒:点击原图中背景区域,擦除相连相似色
- 🎨 色键:指定颜色扣除(未拾色时自动识别外沿 4px 边框的众数颜色=任意纯色底)
- 🌐 AI:调用 Replicate rembg(需配置 Key,无 Key 自动回退本地)

步骤
1. 上传图片(点击或拖拽)
2. 选择模式;色键/魔棒可点原图拾色
3. (可选)羽化/描边 参数;「去残留(保留最大主体)」默认开启,自动清理飘絮碎点
4. 「✂️ 一键抠图」→「⬇ 下载 PNG」/「📥 入库」
```

### 3.5 🗺️ 无缝大地图

AI 生成无缝大地图 / 瓦片、无缝化、TileSet 导出、程序化地形、多区块拼接工作台。

```
A. AI 生成
1. 模式:瓦片模式(单块可平铺) / 完整大地图模式(整张场景)
2. 填 Prompt → 选提供商/模型(可带参考图) → 「✨ AI 生成」
3. 「♻ 整图无缝化」优化平铺;「Wang Tiles」生成 2×2 规则瓦片
4. 「🔪 切成 TileSet」→「⬇ 导出 TileSet」(PNG + TileSet.json)
5. 大地图区支持滚轮缩放、拖拽;「⬇ 高清 PNG」「📥 入库」

B. 程序化地形(本地生成,无需 API)
1. 填种子(同种子结果可复现)、海平面、山脉强度
2. 「🧬 生成地形」→ 预览水/河流/陆地/森林/山脉/雪顶 6 类地形
3. 「⬇ 导出 TileSet」(json + 预览 PNG,接地形分类)/「📥 入库」

C. 🧩 无缝拼接工作台(借鉴 MapStitch 构思) —— 无限大地图闭环
1. 生成或上传一张底图 → 「➕ 当前图作新区块」(自动统一 1024px 并按网格向右拼接)
2. 「🔲 提取右/下边缘参考图」下载 → 交给 AI 局部重绘扩展
3. 重绘结果「📁 上传区块图」回拼;用「对齐偏移」滑杆精确对缝
4. 掩码绘制:点 禁足区(红)/遮挡区(蓝)/前景层(黄),在画布上按住拖拽涂画
5. 「📦 导出 Godot 包」→ 各区块 PNG + map_data.json
   (字段与 MapChunkManager.cs 兼容:global_position + global_polygons)
```

### 3.6 🌦️ 场景工坊(天气 × 日夜)

给场景底图叠加 晴天/雨天/雷暴/下雪 与昼夜色调,实时预览。

```
1. 底图三种来源:输入 Prompt +「✨ 生成场景」/「📁 上传场景图」/ 空(本地演示底图)
2. 天气切换:☀️ 晴天 / 🌧️ 雨天 / ⛈️ 雷暴 / ❄️ 下雪
3. 调节:效果强度、风力/飘动、时间(日夜,06:00~23:00)
   —— 清晨/黄昏暖色调、夜晚偏蓝、雨雪为纯程序化粒子绘制
4. 「⬇ 导出当前帧」把当前天气帧保存为 PNG;「📥 入库」到场景库
```

> 提示:同一场景可生成 4 套天气变体(Prompt 自动追加天气描述),配合预览选择最合适的。

### 3.7 📚 素材总管

全管线素材库 + 自建包管理。

```
- 左侧:内置库(角色/序列帧/素材/抠图/瓦片/大地图/场景/后处理)+「➕ 新建包」
  自建包支持 ✎ 重命名、🗑 删除(包内素材自动移回素材库)
- 素材卡片:⬇ 下载 / ⇄ 移动到其他库 / 🗑 删除
- 导入:任意图片格式(PNG/JPG/WebP/GIF/BMP/SVG/AVIF…)
  「⬆ 导入图片/备份」多选 / 「📁 导入文件夹」整目录 / 直接拖图到网格区
- 「⬇ 导出备份」JSON 备份(含图片数据,可迁移);「⬆ 导入备份」恢复
- 搜索框按 编号/名称 过滤当前库
```

### 3.8 ✨ 后处理工坊

调色板量化(像素风)/ 精灵描边 / 尺寸调整。

```
1. 上传或拖入 PNG/JPG
2. 选操作 + 参数(例:16 色 / 描边 2px / 64px)→「✨ 执行」
3. 「⬇ 下载 PNG」/「📥 入库」
```

### 3.9 🔌 API 配置(统一入口)

内置供应商 Key 与第三方自定义路由都在这里配置,保存后自动同步到所有生成面板。

```
A. 内置供应商 Key(顶部卡片)
   OpenAI(DALL·E/GPT-Image)、Stability、Replicate(抠图)、SiliconFlow
   填好后「💾 保存 Key」—— Key 只存本地浏览器,直连提供商

B. 自定义供应商(中转站/官方)
1. 预设名称 → 协议类型(OpenAI 兼容 / Stability 风格 / SiliconFlow 风格)
2. Base URL(可只填到 /v1,系统自动补全 /images/generations)+ API Key
3. 「🔍 获取默认模型」→ 自动拉取该 Key 可用的全部模型,弹出勾选面板
   —— 已选 N 个模型 / 全选 / 全不选 / ✅ 确定;可全用也可只选部分
4. 「➕ 新增/保存」→ 预设出现在所有生成面板的提供商下拉中;
   选中该供应商时下方出现模型下拉,列出你勾选的模型,可自由切换
5. 列表项支持 编辑 / 试生成 / 删除
```

### 3.10 ⚙️ 设置 / 导出

- 项目名 + 「📄 生成 manifest.json」:记录 Godot 目录结构建议;
- 「📦 打包下载」「清空历史」「查看存储」。

---

## 4. API 与密钥配置

- **BYOK**:Key 仅保存在浏览器 `localStorage`,请求直接发往你填的提供商,**永不上传任何服务器**。
- 支持 OpenAI / Stability AI / Replicate / SiliconFlow / 任意 OpenAI 兼容中转。
- 生图请求默认 `response_format=b64_json`(图片本地化,摆脱跨域),供应商不支持时自动回退重试。
- 无 Key 也能玩:所有生成面板都有「本地演示(无Key)」,用 Canvas 占位图走通全流程。

---

## 5. 素材管理与导出

- 所有素材可「📥 入库」进素材总管(IndexedDB 本地持久化,刷新不丢);
- 每模块「⬇ 下载」为可靠下载:data/blob 直接存盘,远程图自动走代理/新标签兜底,绝不覆盖当前页面;
- 导出中心历史记录最近 100 条生成,可生成 `manifest.json` 或打包下载;
- 素材库可整体导出 JSON 备份,换机器导入恢复。

---

## 6. Godot 导入要点

| 素材 | Godot 建议 |
|---|---|
| 角色立绘 | `Texture2D` → `Sprite2D`;像素风设 `Filter: Nearest`,关 Mipmaps |
| 序列帧(方向动画) | 导出的 `SpriteFrames.tres` 与 PNG **同目录拖入即用**(含 down/left/up/right + stand 动画);`SpriteFrames.json` 为辅助数据 |
| 瓦片/大地图 | 导出的 `TileSet.tres` 与 PNG **同目录拖入即用**(自动按行列分区);无缝图可直接做场景背景 |
| 拼接工作台导出 | 解压导出包到 `res://`,挂载 `MapChunkManager.cs`(Map Data Path 指向 `map_data.json`),自动生成地块/建筑/碰撞;`global_polygons` 的 col=碰撞、occ=遮挡半透明、fg=前景压层 |
| 程序化地形 TileSet | `procedural_tileset_*.tres` 与地形 PNG **同目录拖入即用**(256 格自动映射);`TileSet.json` 为辅助数据 |
| 抠图/程序化素材 | 直接拖入 `res://assets/` |

---

## 7. 数据与隐私

- 素材库:浏览器 **IndexedDB**(`godot-arter-assets`);
- 配置/历史/API Key / 自定义预设:浏览器 **localStorage**;
- 所有数据本地保存,断开网络仍可用(注意:Key 若未手动备份,清浏览器数据会丢失,请妥善保管自己的 Key)。

---

## 8. 常见问题 FAQ

**Q: 生成了图片但预览空白?**
A: 多为旧版跨域图片兜底问题。请强制刷新(Ctrl+F5);优先使用 b64_json 的中转站,或改用 `node server.mjs` 本地服务方式打开。

**Q: 「获取默认模型」没反应或报错?**
A: 先确认 Base URL + API Key 已填;报 `HTTP 401/403` 说明 Key 无效;`Failed to fetch` 说明中转站不允许跨域(换允许 CORS 的中转站或用本地服务方式)。

**Q: 下载 PNG 变成新页面打开?**
A: 这是旧行为,现已改为可靠下载;若仍出现请硬刷新。远程图在 `file://` 直开且图床禁跨域时,下载会兜底为新标签打开原图(显示不受影响),此时推荐用本地服务方式。

**Q: 抠图后出现残留碎点?**
A: 勾选「去残留(保留最大主体)」即可自动清理;色键模式下自动识别纯色底(无需手动拾色),复杂背景建议用魔棒或 AI 模式。

**Q: 天气画布不显示雨雪?**
A: 确认「效果强度」不为 0;底图为空时会显示本地演示底图,雨雪是纯程序化粒子,无需素材。

**Q: 拼接工作台怎么让两块图无缝?**
A: 工作流是闭环:区块 → 「提取右/下边缘参考图」→ AI 按参考图局部重绘出扩展图 → 「上传区块图」回拼 → 用「对齐偏移」滑杆微调到像素级对齐。

**Q: 序列帧导出后 Godot 方向不对?**
A: 勾选「行 = 方向」并正确填写方向名(如 down,left,up,right),导出会附带 `SpriteFrames.tres`——与 PNG 放同一目录拖入 Godot 即自动生成带 down/left/up/right + stand 动画的 SpriteFrames 资源,无需手动配置。

---

> 本说明书随项目一起发布,欢迎在 [GitHub](https://github.com/dk666-big/Godot-Arter) 提 issue 反馈问题与改进建议。